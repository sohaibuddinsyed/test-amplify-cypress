version: 1
frontend:
  phases:
    build:
      commands:
        - npm install
        - npm install --save-dev cypress
        - ls -al
        - ls -al ~/.cache
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - .npm/**/*
      - node_modules/**/*
